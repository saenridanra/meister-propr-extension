/**
 * Testbed mock for azure-devops-extension-api/Git.
 * The real module exports GitRestClient; this stub is used only as an
 * identifier passed to getClient() — the actual mock client lives in
 * azure-devops-extension-api.ts.
 */

export class GitRestClient {}
